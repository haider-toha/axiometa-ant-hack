#pragma once
// ==========================================================================
//  secrets.example.h — committed template. Copy to secrets.h and fill in.
//  secrets.h is git-ignored (**/secrets.h in repo root .gitignore).
//
//  VERCEL_HOST is the bare host (no scheme, no trailing slash); net.cpp
//  prepends "https://" and appends the /api/... path.
// ==========================================================================
// Use a 2.4 GHz-compatible phone hotspot. On iPhone enable Maximize
// Compatibility; on Android select the 2.4 GHz hotspot band.
#define WIFI_SSID   "your-phone-hotspot-ssid"
#define WIFI_PASS   "your-phone-hotspot-password"
#define VERCEL_HOST "bus-stop-awareness.vercel.app"
